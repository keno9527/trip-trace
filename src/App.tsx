export default function App() {
  return (
    <main className="app-shell">
      <section className="empty-state" aria-labelledby="app-title">
        <h1 id="app-title">Trip Trace</h1>
        <button type="button">创建第一趟旅行</button>
      </section>
    </main>
  );
}
